# 演化日志

## 2026-04-21

- 初始化 `codex-readme/` 最小可运行骨架，并生成根级 `AGENTS.md`、前后端子级 `AGENTS.md`。
- 按当前仓库真实结构拆分 `project/`、`rules/`、`hooks/`、`agents/`、`memory/`，避免把既有 `docs/*.md` 复制成第二份事实来源。
- 为多应用工作区补充了路径规则（前端、后端、文档/SQL、运维部署）和高风险场景规则（鉴权、公开态、附件、Redis、部署）。
- 写入首批 seed memory，用于后续把稳定经验晋升到规则层。

### 触发原因

- 当前仓库缺少统一的任务装载入口，且跨前后端、Redis、Nginx、SQL 的高风险链路较多。

### 后续触发条件

- 新增关键目录需要独立路径规则。
- 新风险链路出现重复失败模式。
- 现有规则与真实实现出现偏差，需要从 `memory/` 晋升或回收规则。
