# 写作与语言规则

## 默认语言

- 治理文档、任务说明、复盘默认使用中文。
- 代码、接口、环境变量、数据库字段、类型名保持原样。

## 写作约束

- 优先写可执行信息，不写空泛口号。
- 能引用现有权威文档时，直接给路径，不重复抄录长段内容。
- 无法从仓库确认的内容必须显式标注“假设”。
- `Verify` 只写做过的验证；未做的写成缺口，不伪装成已完成。

## 编码

- 文本统一 UTF-8，遵循 `.editorconfig`。
- 处理 Windows 终端乱码时，以文件实际编码为准，不以终端显示误判为文件损坏。
